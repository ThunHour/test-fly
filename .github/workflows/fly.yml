name: Fly Deploy
on:
  push:
    branches:
      - main
env:
  FLY_API_TOKEN: ${{ "tMXDi7bqVYKd1cbm-k0BOCZ4sw9h1w3XKpehqopiudE" }}
jobs:
  deploy:
      name: appe-with-db
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - uses: superfly/flyctl-actions/setup-flyctl@master
        - run: flyctl deploy --remote-only
